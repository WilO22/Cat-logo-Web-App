rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Permite la lectura y escritura si el usuario NO ha iniciado sesión.
      // Esto es lo que cambiaremos.
      allow read, write: if false;
    }
  }
}